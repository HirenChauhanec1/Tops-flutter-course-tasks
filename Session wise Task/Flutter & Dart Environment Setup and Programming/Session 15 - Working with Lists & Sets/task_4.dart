void main(){

  // 4.
  // Create a Dart set called myPlaylist containing 4 unique song names (as strings),
  // including one duplicate song. Print the set and explain in a comment what happened to the duplicate.
  // <br><br><em><strong>Hint:</strong> Sets automatically prevent duplicates.</em>

  // A Set stores unique elements.if we try to add a duplicate element, Dart ignores it.
  Set<String> myPlaylist = {"Humnava","Dhun","Humdard","Khairiyat","Dhun"};
  print(myPlaylist);


}