import 'dart:io';

void main() {
  // 5.
  // Rewrite your variable declarations in main.dart to use explicit types instead of var,
  // and add a comment above each variable explaining why that data type is appropriate.
  // <br><br><em><strong>Hint:</strong> For example, use int for age because it never has decimals.</em>


  // Use String for word like Hiren because a String is a collection of characters.
  String name = "Hiren";

  // Use int for age because age is stored as a whole number, not a decimal number.
  int age = 31;

  // Use String for word like chauhanhirenec1@gmail.com because a String is a collection of characters.
  String email = "chauhanhirenec1@gmail.com";

}
