
void main(){

  // Define a Subject class with fields code (String), name (String), and isElective (bool), and a
  // constructor that initialises all three fields.

}

class Subject {
  String code, name;
  bool isElective;

  Subject({
    required this.code,
    required this.name,
    required this.isElective,
  });
}